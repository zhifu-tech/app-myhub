# AI 捕获系统视觉化架构图（端到端）

```
┌───────────────────────────────────────────────┐
│                    Client Layer               │
│ ┌───────────────────────┐ ┌─────────────────┐ │
│ │ Chat Interface        │ │ Card Preview    │ │
│ │ - Conversation        │ │ - Structured UI │ │
│ │ - Action Components   │ │ - Live Update   │ │
│ │ - Quick Actions       │ │ - Media Display │ │
│ └───────────────────────┘ └─────────────────┘ │
└───────────────────────────┬───────────────────┘
                            │
                            ▼
                ┌────────────────────────┐
                │      API Gateway       │
                │ - Auth                │
                │ - Rate Limit          │
                │ - Logging / Tracing   │
                └──────────┬───────────┘
                           │
                           ▼
        ┌──────────────────────────────────────┐
        │       Conversation Engine            │
        │--------------------------------------│
        │ - State Machine（流程控制）           │
        │ - State Guard（状态约束）            │
        │ - Context Manager（上下文）          │
        │ - Slot Manager（字段管理）           │
        │ - Action Planner（下一步决策）       │
        └──────────┬──────────────────────────┘
                   │（控制流）
                   ▼
        ┌──────────────────────────────────────┐
        │          LLM Agent Layer             │
        │--------------------------------------│
        │ Prompt System                        │
        │  • System Prompt（规则）             │
        │  • Task Prompt（状态+上下文）        │
        │  • Tool Prompt（工具协议）           │
        │                                      │
        │ Reasoning & Decision                 │
        │ Tool Selection（JSON输出）           │
        └──────────┬──────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │        Tool Orchestrator（新增）      │
        │--------------------------------------│
        │ - Tool Router（路由）                │
        │ - Tool Registry（工具注册）          │
        │ - Param Validator（参数校验）        │
        │ - Idempotency（幂等控制）            │
        │ - Retry / Fallback                  │
        │ - Tool Chain Executor（工具链）      │
        └──────────┬──────────────────────────┘
                   │（执行流）
                   ▼
        ┌──────────────────────────────────────┐
        │             Tool Layer               │
        │--------------------------------------│
        │ - create_card                        │
        │ - update_card                        │
        │ - publish_card                       │
        │ - suggest_tags                       │
        │ - extract_entities                   │
        │ - summarize_content                 │
        │ - analyze_content / image           │
        │ - search_cards                      │
        │ - retrieve_similar_cards（RAG）      │
        └──────────┬──────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │             Card Engine              │
        │--------------------------------------│
        │ - Card CRUD（唯一写入口）            │
        │ - Schema Validator（强校验）         │
        │ - Field Formatter                   │
        │ - Versioning（版本控制）             │
        │ - Permission Control                │
        └──────────┬──────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │            Storage Layer             │
        │--------------------------------------│
        │ - Primary DB（cards / conversations）│
        │ - Vector DB（Embedding / Search）    │
        │ - File Storage（Image / Media）      │
        │ - Cache（Redis / State / Session）   │
        └──────────┬──────────────────────────┘
                   │
                   ▼
        ┌──────────────────────────────────────┐
        │       Knowledge Organization Layer   │
        │--------------------------------------│
        │ - Universal Card Schema              │
        │ - Card Type System                   │
        │ - Knowledge Graph（关系）            │
        │ - Smart Collections（动态集合）      │
        │ - Multi-dimensional Views            │
        └──────────────────────────────────────┘

───────────────────────────────────────────────

（侧向能力：跨层支撑）

        ┌──────────────────────────────────────┐
        │     Async & Infra Layer（新增）      │
        │--------------------------------------│
        │ - Queue（AI任务异步化）              │
        │ - Streaming（流式响应）              │
        │ - Event Bus（状态变更通知）          │
        │ - Scheduler（延迟任务）              │
        └──────────────────────────────────────┘

        ┌──────────────────────────────────────┐
        │     Observability（可观测性）        │
        │--------------------------------------│
        │ - Logs（对话 / Tool / AI）           │
        │ - Metrics（成功率 / 延迟）           │
        │ - Tracing（调用链路）                │
        │ - Error Tracking                    │
        └──────────────────────────────────────┘

        ┌──────────────────────────────────────┐
        │        Response Builder（新增）      │
        │--------------------------------------│
        │ - Message Generator                  │
        │ - UI Component Builder              │
        │ - Card Preview Builder              │
        │ - State-aware Rendering             │
        └──────────────────────────────────────┘

```
