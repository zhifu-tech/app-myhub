# AI Capture

## TopAppbar

1. UI 效果
    - background： 玻璃的模糊效果，和 searchbar 对齐
    - Leading button: back button
    - Headline: “AI 捕获”
    - Subtitle： 图标 + label 显示在线状态
    - 右侧：隐藏

## Bottom bar

示意样式

```
+------------------------------------------------+ |
|                                                  |
| [       输入框。         ]               [发送]    |
+------------------------------------------------+

```

1. 底部输入框：
    - background： 玻璃的模糊效果，和 searchbar 对齐
    - 左侧： 输入框, 位置和大小和 search bar 对齐
2. 发送按钮，
    - 右侧： 发送按钮（纸飞机），在输入框以外。

## 内容区

1. AI 助手的图标，替换为 logo。

2. 预览区域
   2.1. 预览内容

```
┌───────────────────┐
│ Card Preview      │
│                   │
│ Title             │
│ 一兰拉面          │
│                   │
│ Location          │
│ 上海              │
│                   │
│ Tags              │
│ 美食  拉面        │
│                   │
│ Image             │
│ [image preview]   │
│                   │
│ Notes             │
│ 用户备注内容       │
└───────────────────┘
```

2.2. 显示方式

- 以 side sheets 的形式显示在左侧。在手机端 默认隐藏。
- 手机端，默认会在 发送按钮上方显示一个缩略图

```
+------------------------------------------------+ |
|                                                  |
|                                     [Preview]    |
|                                     [ 缩略图 ]    |
|                                                  |
| [       输入框。         ]               [发送]    |
+------------------------------------------------+

```

2.3. 预览图

- 卡片预览区域的的等比例缩放
- 只显示缩略图，不需要 文字指引。
- 有上下跳动的动画（心跳），提示用户可点击。


1. topbar

- 移除 顶部 status bar， 设计时，不需要考虑。

2. 内容区：

- AI 头像 采用 我们的 "Kinetic Flow" logo,（focusing on that fluid gradient 'M' symbol ，已有，不要新设计）
- 提示上传图片和添加标签 需要拆分为两条数据

   ```
   xxxx(其他描述）, 需要添加一张图片吗？

   [上传图片]
   [跳过]
   ```

    ```
    xxxx(其他描述）,要不要加个标签？
    
    [美食] （需要有删除按钮，用户可以移除改标签）
    [餐厅]（需要有删除按钮，用户可以移除改标签）
    [日本料理]（需要有删除按钮，用户可以移除改标签）
    [下一步]
    ```

    ```
    xxxx(其他描述）,卡片准备好了 👇
    是否发布？
    [发布]
    [继续编辑]
    ```

3. 预览区域
    - 移除 Preview 的顶部 label。
    - 增加 上下跳动的动画（心跳），提示用户可点击。
4. bottom
    - 当没有输入内容时，【发送】按钮，不可点击


1. topbar
    - 无需修改
2. 内容区
    - 上传图片
    - 添加标签
        - 修改[跳过] 为 [下一步]
    - 输入标题 （新增：需要插入一条消息：输入标题）
        ```
        xxxx(其他描述）,增加
        是否发布？
        [发布]
        [继续编辑]
       ```
   - 发布
3. bottom
    - 增加 上传图片的 按钮，用户可以选择上传图片
      - 只增加 图片图标
    ```
   | [上传图片icon] [       输入框。         ]               [发送]    |
   ```


1. top
- 移除 CARD PREVIEW 文案 和 close 按钮
- 左侧：笔记图标  
- 右侧：发布时间，如: March 12, 2026
2. content
- 保持不变
3. 底部，移除“确认并发布”按钮。 


原有：137FEC
