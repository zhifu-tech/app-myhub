# AI 捕获系统 V2 完整请求与返回示例

## 1. 请求示例（AiCaptureRequestV2）

```json
{
  "protocol": {
    "version": "2.0"
  },
  "session": {
    "id": "conv_20260417_0001",
    "turn_id": 12
  },
  "draft": {
    "version": 5,
    "data": {
      "title": "",
      "summary": "周末在静安寺附近吃到一家不错的拉面店。",
      "tags": [
        "美食"
      ],
      "media": [
        {
          "id": "media_01",
          "kind": "image",
          "uri": "content://media/external/images/media/12345"
        }
      ],
      "extra": {
        "capture.source": "chat"
      }
    }
  },
  "constraints": {
    "required_fields": [
      "title",
      "tags"
    ],
    "locked_fields": [],
    "field_rules": {
      "title": {
        "max_len": 80
      },
      "summary": {
        "max_len": 500
      },
      "tags": {
        "max_count": 10
      }
    },
    "conflict_policy": {
      "title": "user_wins",
      "tags": "merge",
      "summary": "append"
    }
  },
  "input": {
    "text": "帮我整理成一张卡片，标题突出拉面店，标签再补两个",
    "attachments": []
  },
  "context": {
    "short_history": "用户正在整理一条美食体验记录，当前缺少明确标题，标签较少。",
    "active_fields": [
      "title",
      "tags"
    ],
    "language": "zh-CN",
    "key_facts": {
      "runtime_context": {
        "now": "2026-04-17T11:30:00+08:00",
        "timezone": "Asia/Shanghai",
        "location": {
          "name": "Shanghai",
          "source": "gps"
        }
      },
      "persona_profile": {
        "name": "Milo",
        "tone": "专业、简洁、友好",
        "verbosity": "short"
      },
      "tool_capability": [
        "update_title",
        "update_tags",
        "add_media"
      ]
    }
  }
}
```

## 2. 返回示例（AiCaptureResponseV2）

```json
{
  "proposals": {
    "patches": [
      {
        "op": "replace",
        "path": "/title",
        "value": "静安寺周末拉面店体验记录"
      },
      {
        "op": "add",
        "path": "/tags/-",
        "value": "拉面"
      },
      {
        "op": "add",
        "path": "/tags/-",
        "value": "餐厅"
      }
    ],
    "clarify": []
  },
  "signals": {
    "publish_ready": true,
    "needs_clarification": false
  },
  "message": "我已补充标题和标签。",
  "suggestions": [
    {
      "type": "add_media",
      "priority": "high",
      "field": "media",
      "reason": "visual_first"
    }
  ]
}
```

## 3. 对应关系说明

- 请求和返回字段均符合 `AiCaptureRequestV2 / AiCaptureResponseV2` 结构。
- 返回仅提供候选提议，不包含 `intent/state/decision`。
- `key_facts` 用于承载运行时上下文和人设配置，保持协议主干稳定。

## 4. 推荐提示词输入（简化版）

### 4.1 System Prompt（目标驱动）

```text
你是“结构化捕获助手”，只生成 AiCaptureResponseV2。

优先级：
1) 先补 active_fields，再补 required_fields 中仍缺失的字段。
2) 能从输入安全提炼的 title、tags、summary，直接输出 patches。
3) 只有缺少无法可靠推断的关键信息时才输出 clarify。
4) 非阻塞增强放到 suggestions。

硬约束：
- 只输出一个 JSON 对象，不要解释、不要回显请求、不要额外字段。
- 顶层只允许 proposals、signals、message、suggestions。
- 仅修改 /title、/summary、/tags、/extra/*；媒体相关动作只通过 suggestions 表达，不要输出 /media patch。
- /title、/summary 的 value 必须是字符串；/tags、/tags/- 的 value 必须是字符串或字符串数组。
- message 只能描述真实 patch / clarify / suggestions 结果。
- 仅 suggest_tags、set_location、set_capture_type 允许 payload；其余 suggestion 不要携带 payload。
```

### 4.2 User Prompt（直接传 compact request JSON）

```json
{
  "protocol": {
    "version": "2.0"
  },
  "session": {
    "id": "conv_20260417_0001",
    "turn_id": 12
  },
  "draft": {
    "version": 5,
    "data": {
      "title": "",
      "summary": "周末在静安寺附近吃到一家不错的拉面店。",
      "tags": ["美食"],
      "media": [
        {
          "id": "media_01",
          "kind": "image",
          "uri": "content://media/external/images/media/12345"
        }
      ],
      "extra": {
        "capture.source": "chat"
      }
    }
  },
  "constraints": {
    "required_fields": ["title", "tags"],
    "locked_fields": [],
    "field_rules": {
      "title": { "max_len": 80 },
      "summary": { "max_len": 500 },
      "tags": { "max_count": 10 }
    },
    "conflict_policy": {
      "title": "user_wins",
      "tags": "merge",
      "summary": "append"
    }
  },
  "input": {
    "text": "帮我整理成一张卡片，标题突出拉面店，标签再补两个",
    "attachments": []
  },
  "context": {
    "short_history": "用户正在整理一条美食体验记录，当前缺少明确标题，标签较少。",
    "active_fields": ["title", "tags"],
    "language": "zh-CN",
    "key_facts": {
      "runtime_context": {
        "now": "2026-04-17T11:30:00+08:00",
        "timezone": "Asia/Shanghai"
      }
    }
  }
}
```
